import 'package:flutter/material.dart';

class HikeDetails extends StatelessWidget {
  const HikeDetails({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Scaffold(
        body: Center(child: Text("Hike details")),
      ),
    );
  }
}