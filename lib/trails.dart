import 'package:flutter/material.dart';

class Trails extends StatelessWidget {
  const Trails({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Scaffold(
        body: Center(child: Text("Trails")),
      ),
    );
  }
}